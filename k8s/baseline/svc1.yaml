apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc1
  labels:
    app: svc1
    env: baseline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc1
      env: baseline
  template:
    metadata:
      labels:
        app: svc1
        env: baseline
    spec:
      containers:
        - name: svc1
          image: ghcr.io/bxl1015/gitops-lane-svc1:main
          imagePullPolicy: Always
          env:
            - name: SERVICE_NAME
              value: svc1
            - name: SERVICE_ENV
              value: baseline
          resources:
            limits:
              memory: 128Mi
            requests:
              memory: 64Mi
          ports:
            - name: http
              containerPort: 9000
---
apiVersion: v1
kind: Service
metadata:
  name: svc1
  labels:
    app: svc1
spec:
  selector:
    app: svc1
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
