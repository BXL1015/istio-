apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc10
  labels:
    app: svc10
    env: baseline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc10
      env: baseline
  template:
    metadata:
      labels:
        app: svc10
        env: baseline
    spec:
      containers:
        - name: svc10
          image: ghcr.io/bxl1015/gitops-lane-svc10:main
          imagePullPolicy: Always
          env:
            - name: SERVICE_NAME
              value: svc10
            - name: SERVICE_ENV
              value: baseline
          resources:
            limits:
              memory: 128Mi
            requests:
              memory: 64Mi
          ports:
            - name: http
              containerPort: 9000
---
apiVersion: v1
kind: Service
metadata:
  name: svc10
  labels:
    app: svc10
spec:
  selector:
    app: svc10
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
