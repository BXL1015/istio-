apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc3
  labels:
    app: svc3
    env: baseline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc3
      env: baseline
  template:
    metadata:
      labels:
        app: svc3
        env: baseline
    spec:
      containers:
        - name: svc3
          image: ghcr.io/bxl1015/gitops-lane-svc3:main
          imagePullPolicy: Always
          env:
            - name: SERVICE_NAME
              value: svc3
            - name: SERVICE_ENV
              value: baseline
          resources:
            limits:
              memory: 128Mi
            requests:
              memory: 64Mi
          ports:
            - name: http
              containerPort: 9000
---
apiVersion: v1
kind: Service
metadata:
  name: svc3
  labels:
    app: svc3
spec:
  selector:
    app: svc3
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
