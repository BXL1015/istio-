apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc8
  labels:
    app: svc8
    env: baseline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc8
      env: baseline
  template:
    metadata:
      labels:
        app: svc8
        env: baseline
    spec:
      containers:
        - name: svc8
          image: ghcr.io/bxl1015/gitops-lane-svc8:main
          imagePullPolicy: Always
          env:
            - name: SERVICE_NAME
              value: svc8
            - name: SERVICE_ENV
              value: baseline
          resources:
            limits:
              memory: 128Mi
            requests:
              memory: 64Mi
          ports:
            - name: http
              containerPort: 9000
---
apiVersion: v1
kind: Service
metadata:
  name: svc8
  labels:
    app: svc8
spec:
  selector:
    app: svc8
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
