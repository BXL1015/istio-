apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc15
  labels:
    app: svc15
    env: baseline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc15
      env: baseline
  template:
    metadata:
      labels:
        app: svc15
        env: baseline
    spec:
      containers:
        - name: svc15
          image: ghcr.io/bxl1015/gitops-lane-svc15:main
          imagePullPolicy: Always
          env:
            - name: SERVICE_NAME
              value: svc15
            - name: SERVICE_ENV
              value: baseline
          resources:
            limits:
              memory: 128Mi
            requests:
              memory: 64Mi
          ports:
            - name: http
              containerPort: 9000
---
apiVersion: v1
kind: Service
metadata:
  name: svc15
  labels:
    app: svc15
spec:
  selector:
    app: svc15
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
