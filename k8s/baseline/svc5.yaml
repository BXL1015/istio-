apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc5
  labels:
    app: svc5
    env: baseline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc5
      env: baseline
  template:
    metadata:
      labels:
        app: svc5
        env: baseline
    spec:
      containers:
        - name: svc5
          image: ghcr.io/bxl1015/gitops-lane-svc5:main
          imagePullPolicy: Always
          env:
            - name: SERVICE_NAME
              value: svc5
            - name: SERVICE_ENV
              value: baseline
          resources:
            limits:
              memory: 128Mi
            requests:
              memory: 64Mi
          ports:
            - name: http
              containerPort: 9000
---
apiVersion: v1
kind: Service
metadata:
  name: svc5
  labels:
    app: svc5
spec:
  selector:
    app: svc5
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
