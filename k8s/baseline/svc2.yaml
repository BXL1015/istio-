apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc2
  labels:
    app: svc2
    env: baseline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc2
      env: baseline
  template:
    metadata:
      labels:
        app: svc2
        env: baseline
    spec:
      containers:
        - name: svc2
          image: ghcr.io/bxl1015/gitops-lane-svc2:main
          imagePullPolicy: Always
          env:
            - name: SERVICE_NAME
              value: svc2
            - name: SERVICE_ENV
              value: baseline
          resources:
            limits:
              memory: 128Mi
            requests:
              memory: 64Mi
          ports:
            - name: http
              containerPort: 9000
---
apiVersion: v1
kind: Service
metadata:
  name: svc2
  labels:
    app: svc2
spec:
  selector:
    app: svc2
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
