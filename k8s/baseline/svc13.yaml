apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc13
  labels:
    app: svc13
    env: baseline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc13
      env: baseline
  template:
    metadata:
      labels:
        app: svc13
        env: baseline
    spec:
      containers:
        - name: svc13
          image: ghcr.io/bxl1015/gitops-lane-svc13:main
          imagePullPolicy: Always
          env:
            - name: SERVICE_NAME
              value: svc13
            - name: SERVICE_ENV
              value: baseline
          resources:
            limits:
              memory: 128Mi
            requests:
              memory: 64Mi
          ports:
            - name: http
              containerPort: 9000
---
apiVersion: v1
kind: Service
metadata:
  name: svc13
  labels:
    app: svc13
spec:
  selector:
    app: svc13
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
