apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc6
  labels:
    app: svc6
    env: baseline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc6
      env: baseline
  template:
    metadata:
      labels:
        app: svc6
        env: baseline
    spec:
      containers:
        - name: svc6
          image: ghcr.io/bxl1015/gitops-lane-svc6:main
          imagePullPolicy: Always
          env:
            - name: SERVICE_NAME
              value: svc6
            - name: SERVICE_ENV
              value: baseline
          resources:
            limits:
              memory: 128Mi
            requests:
              memory: 64Mi
          ports:
            - name: http
              containerPort: 9000
---
apiVersion: v1
kind: Service
metadata:
  name: svc6
  labels:
    app: svc6
spec:
  selector:
    app: svc6
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
