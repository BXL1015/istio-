apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc12
  labels:
    app: svc12
    env: baseline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc12
      env: baseline
  template:
    metadata:
      labels:
        app: svc12
        env: baseline
    spec:
      containers:
        - name: svc12
          image: ghcr.io/bxl1015/gitops-lane-svc12:main
          imagePullPolicy: Always
          env:
            - name: SERVICE_NAME
              value: svc12
            - name: SERVICE_ENV
              value: baseline
          resources:
            limits:
              memory: 128Mi
            requests:
              memory: 64Mi
          ports:
            - name: http
              containerPort: 9000
---
apiVersion: v1
kind: Service
metadata:
  name: svc12
  labels:
    app: svc12
spec:
  selector:
    app: svc12
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
