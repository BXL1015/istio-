apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc14
  labels:
    app: svc14
    env: baseline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc14
      env: baseline
  template:
    metadata:
      labels:
        app: svc14
        env: baseline
    spec:
      containers:
        - name: svc14
          image: ghcr.io/bxl1015/gitops-lane-svc14:main
          imagePullPolicy: Always
          env:
            - name: SERVICE_NAME
              value: svc14
            - name: SERVICE_ENV
              value: baseline
          resources:
            limits:
              memory: 128Mi
            requests:
              memory: 64Mi
          ports:
            - name: http
              containerPort: 9000
---
apiVersion: v1
kind: Service
metadata:
  name: svc14
  labels:
    app: svc14
spec:
  selector:
    app: svc14
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
