apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc4
  labels:
    app: svc4
    env: baseline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc4
      env: baseline
  template:
    metadata:
      labels:
        app: svc4
        env: baseline
    spec:
      containers:
        - name: svc4
          image: ghcr.io/bxl1015/gitops-lane-svc4:main
          imagePullPolicy: Always
          env:
            - name: SERVICE_NAME
              value: svc4
            - name: SERVICE_ENV
              value: baseline
          resources:
            limits:
              memory: 128Mi
            requests:
              memory: 64Mi
          ports:
            - name: http
              containerPort: 9000
---
apiVersion: v1
kind: Service
metadata:
  name: svc4
  labels:
    app: svc4
spec:
  selector:
    app: svc4
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
